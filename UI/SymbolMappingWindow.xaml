<Window
    x:Class="Pulse.UI.SymbolMappingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:Pulse.UI"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="Pulse — Symbol Mapping"
    Width="520"
    Height="480"
    MinWidth="400"
    MinHeight="360"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStyle="None"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Pulse;component/UI/Themes/DarkTheme.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Pulse;component/UI/Themes/ElementStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVis" />

            <!-- Search TextBox -->
            <Style x:Key="SearchBoxStyle" TargetType="{x:Type TextBox}">
                <Setter Property="Background"      Value="{DynamicResource SecondaryBackgroundBrush}" />
                <Setter Property="Foreground"      Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="BorderBrush"     Value="{DynamicResource BorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding"         Value="28,4,8,4" />
                <Setter Property="FontSize"        Value="11" />
                <Setter Property="CaretBrush"      Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Hidden" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="#FF0078D7" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Editable ComboBox for symbol selection -->
            <Style x:Key="SymbolComboBoxStyle" TargetType="{x:Type ComboBox}">
                <Setter Property="OverridesDefaultStyle"  Value="True" />
                <Setter Property="Background"             Value="{DynamicResource SecondaryBackgroundBrush}" />
                <Setter Property="Foreground"             Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="BorderBrush"            Value="{DynamicResource BorderBrush}" />
                <Setter Property="BorderThickness"        Value="1" />
                <Setter Property="Height"                 Value="24" />
                <Setter Property="FontSize"               Value="11" />
                <Setter Property="FocusVisualStyle"       Value="{x:Null}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid>
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="20" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="PART_EditableTextBox"
                                                 Grid.Column="0"
                                                 Text="{Binding Path=Text,
                                                                RelativeSource={RelativeSource TemplatedParent},
                                                                UpdateSourceTrigger=PropertyChanged}"
                                                 Background="Transparent"
                                                 Foreground="{TemplateBinding Foreground}"
                                                 CaretBrush="{TemplateBinding Foreground}"
                                                 SelectionBrush="#FF0078D7"
                                                 BorderThickness="0"
                                                 Padding="7,0,2,0"
                                                 VerticalAlignment="Center"
                                                 Focusable="True" />
                                        <Path Grid.Column="1"
                                              Data="M 0,0 L 4,4 L 8,0"
                                              Stroke="{DynamicResource IconForegroundBrush}"
                                              StrokeThickness="1.5" StrokeLineJoin="Round"
                                              Width="8" Height="4"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              IsHitTestVisible="False" />
                                    </Grid>
                                </Border>
                                <!-- Transparent toggle to open dropdown -->
                                <ToggleButton OverridesDefaultStyle="True"
                                              Background="Transparent" BorderThickness="0"
                                              IsChecked="{Binding Path=IsDropDownOpen,
                                                                  RelativeSource={RelativeSource TemplatedParent},
                                                                  Mode=TwoWay}"
                                              Focusable="False" Cursor="Hand">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border Background="Transparent" CornerRadius="4" />
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <!-- Dropdown popup -->
                                <Popup x:Name="PART_Popup"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       Placement="Bottom"
                                       PopupAnimation="Slide">
                                    <Border Background="{DynamicResource SecondaryBackgroundBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            MinWidth="{Binding ActualWidth,
                                                               RelativeSource={RelativeSource AncestorType=Grid}}"
                                            MaxHeight="180"
                                            Margin="0,2,0,0">
                                        <ScrollViewer>
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush"
                                            Value="{DynamicResource IconForegroundBrush}" />
                                </Trigger>
                                <Trigger Property="IsDropDownOpen" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="#FF0078D7" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="ItemContainerStyle">
                    <Setter.Value>
                        <Style TargetType="{x:Type ComboBoxItem}">
                            <Setter Property="OverridesDefaultStyle"      Value="True" />
                            <Setter Property="Background"                 Value="Transparent" />
                            <Setter Property="Foreground"                 Value="{DynamicResource ForegroundBrush}" />
                            <Setter Property="Padding"                    Value="8,5" />
                            <Setter Property="FontSize"                   Value="11" />
                            <Setter Property="FocusVisualStyle"           Value="{x:Null}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                                        <Border x:Name="Bg"
                                                Background="{TemplateBinding Background}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsHighlighted" Value="True">
                                                <Setter TargetName="Bg" Property="Background"
                                                        Value="{DynamicResource HighlightBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Row hover -->
            <Style x:Key="MappingRowStyle" TargetType="Border">
                <Setter Property="Background" Value="Transparent" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#15FFFFFF" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="Transparent">
        <!-- Frosted-glass edge glow -->
        <Border BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="2"
                CornerRadius="10"
                Opacity="0.7"
                SnapsToDevicePixels="True">
            <Border.Effect>
                <BlurEffect KernelType="Gaussian" Radius="2" />
            </Border.Effect>
        </Border>

        <!-- Main surface -->
        <Border Margin="2"
                Background="{DynamicResource PrimaryBackgroundBrush}"
                CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />    <!-- Title bar -->
                    <RowDefinition Height="Auto" />  <!-- Toolbar -->
                    <RowDefinition Height="Auto" />  <!-- Column headers -->
                    <RowDefinition Height="*" />     <!-- Rows -->
                    <RowDefinition Height="Auto" />  <!-- Footer -->
                </Grid.RowDefinitions>

                <!-- ── Title bar ──────────────────────────────────────────── -->
                <ui:TitleBar Grid.Row="0" IsMinimizeVisible="False" />

                <!-- ── Toolbar ────────────────────────────────────────────── -->
                <Border Grid.Row="1"
                        Background="{DynamicResource SecondaryBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="12,7">
                    <DockPanel>
                        <!-- Mapped count -->
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Check" Width="13" Height="13"
                                                     Foreground="#FF6EC26E"
                                                     VerticalAlignment="Center" Margin="0,0,4,0" />
                            <TextBlock FontSize="11" Foreground="{DynamicResource IconForegroundBrush}"
                                       VerticalAlignment="Center">
                                <Run Text="{Binding MappedCount, Mode=OneWay}" />
                                <Run Text=" / " />
                                <Run Text="{Binding TotalCount, Mode=OneWay}" />
                                <Run Text=" mapped" />
                            </TextBlock>
                        </StackPanel>

                        <!-- + New Symbol button -->
                        <Button DockPanel.Dock="Right"
                                Style="{StaticResource PulseActionButtonStyle}"
                                Command="{Binding CreateNewSymbolCommand}"
                                ToolTip="Design a new custom symbol"
                                Padding="8,4"
                                Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Plus" Width="12" Height="12"
                                                         VerticalAlignment="Center" Margin="0,0,4,0" />
                                <TextBlock Text="New Symbol" FontSize="11" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <!-- Search box -->
                        <Grid DockPanel.Dock="Left">
                            <TextBox Style="{StaticResource SearchBoxStyle}"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Width="220"
                                     VerticalAlignment="Center" />
                            <materialDesign:PackIcon Kind="Magnify" Width="13" Height="13"
                                                     Foreground="{DynamicResource IconForegroundBrush}"
                                                     VerticalAlignment="Center"
                                                     HorizontalAlignment="Left"
                                                     Margin="8,0,0,0"
                                                     IsHitTestVisible="False" />
                        </Grid>
                    </DockPanel>
                </Border>

                <!-- ── Column headers ─────────────────────────────────────── -->
                <Border Grid.Row="2"
                        Background="{DynamicResource SecondaryBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="12,5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />    <!-- Device Type -->
                            <ColumnDefinition Width="52" />   <!-- Count -->
                            <ColumnDefinition Width="180" />  <!-- Symbol -->
                            <ColumnDefinition Width="28" />   <!-- Edit -->
                            <ColumnDefinition Width="28" />   <!-- Clear -->
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="DEVICE TYPE" FontSize="10" FontWeight="SemiBold"
                                   Foreground="{DynamicResource IconForegroundBrush}" />
                        <TextBlock Grid.Column="1" Text="COUNT" FontSize="10" FontWeight="SemiBold"
                                   Foreground="{DynamicResource IconForegroundBrush}" Margin="4,0,0,0" />
                        <TextBlock Grid.Column="2" Text="SYMBOL" FontSize="10" FontWeight="SemiBold"
                                   Foreground="{DynamicResource IconForegroundBrush}" Margin="8,0,0,0" />
                    </Grid>
                </Border>

                <!-- ── Rows ───────────────────────────────────────────────── -->
                <ScrollViewer Grid.Row="3"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding FilteredEntries}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource MappingRowStyle}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="0,0,0,1"
                                        Padding="12,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="52" />
                                            <ColumnDefinition Width="180" />
                                            <ColumnDefinition Width="28" />
                                            <ColumnDefinition Width="28" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Device Type -->
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding DeviceType}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource ForegroundBrush}"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" />

                                        <!-- Count -->
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Count}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource IconForegroundBrush}"
                                                   VerticalAlignment="Center"
                                                   Margin="4,0,0,0" />

                                        <!-- Symbol ComboBox (editable) -->
                                        <ComboBox Grid.Column="2"
                                                  Style="{StaticResource SymbolComboBoxStyle}"
                                                  IsEditable="True"
                                                  Text="{Binding SymbolName, UpdateSourceTrigger=PropertyChanged}"
                                                  ItemsSource="{Binding DataContext.AvailableSymbols,
                                                               RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                  Margin="8,1,4,1"
                                                  VerticalAlignment="Center" />

                                        <!-- Edit button (visible only when a symbol is assigned) -->
                                        <Button Grid.Column="3"
                                                Command="{Binding DataContext.EditSymbolCommand,
                                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding SymbolName}"
                                                Visibility="{Binding HasSymbol, Converter={StaticResource BoolToVis}}"
                                                Style="{StaticResource PulseActionButtonStyle}"
                                                Padding="2"
                                                VerticalAlignment="Center"
                                                ToolTip="Edit this symbol in the designer">
                                            <materialDesign:PackIcon Kind="Pencil" Width="11" Height="11" />
                                        </Button>

                                        <!-- Clear button -->
                                        <Button Grid.Column="4"
                                                Command="{Binding ClearSymbolCommand}"
                                                Visibility="{Binding HasSymbol, Converter={StaticResource BoolToVis}}"
                                                Style="{StaticResource PulseActionButtonStyle}"
                                                Padding="2"
                                                VerticalAlignment="Center"
                                                ToolTip="Clear symbol">
                                            <materialDesign:PackIcon Kind="Close" Width="11" Height="11" />
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- ── Footer ────────────────────────────────────────────── -->
                <Border Grid.Row="4"
                        Background="{DynamicResource SecondaryBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,1,0,0"
                        CornerRadius="0,0,8,8"
                        Padding="12,8">
                    <DockPanel>
                        <Button Style="{StaticResource PulseActionButtonStyle}"
                                Command="{Binding ClearAllCommand}"
                                ToolTip="Remove all symbol assignments"
                                DockPanel.Dock="Left">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Delete" Width="13" Height="13"
                                                         VerticalAlignment="Center" Margin="0,0,4,0" />
                                <TextBlock Text="Clear All" FontSize="11" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                            <Button Style="{StaticResource PulseActionButtonStyle}"
                                    Command="{Binding CancelCommand}"
                                    Margin="0,0,8,0">
                                <TextBlock Text="Cancel" FontSize="11" />
                            </Button>
                            <Button Style="{StaticResource PulsePrimaryButtonStyle}"
                                    Command="{Binding SaveCommand}"
                                    ToolTip="Save symbol mappings to this Revit document">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ContentSave" Width="13" Height="13"
                                                             VerticalAlignment="Center" Margin="0,0,4,0" />
                                    <TextBlock Text="Save" FontSize="11" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </DockPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
