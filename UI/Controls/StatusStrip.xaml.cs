using System.Windows.Controls;

namespace Pulse.UI.Controls
{
    /// <summary>
    /// Status strip at the bottom of the main window.
    /// Shows status text, device count, warning count, and error count.
    /// </summary>
    public partial class StatusStrip : UserControl
    {
        public StatusStrip()
        {
            InitializeComponent();
        }
    }
}
